import React from 'react';
import PropTypes from 'prop-types';
import Header from './components/Header';

Auth.propTypes = {
    
};

function Auth(props) {
    return (
        <div className='body'>
            <Header/>
        </div>
    );
}

export default Auth;