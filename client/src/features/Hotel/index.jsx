import React from 'react';
import PropTypes from 'prop-types';

Hotel.propTypes = {

};

function Hotel(props) {
    return (
        <div>
            Hotel route
        </div>
    );
}

export default Hotel;